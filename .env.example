# Supabase environment variables for AetherFly
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key-placeholder
VITE_AVIATIONSTACK_API_KEY=your-aviationstack-api-key

# Add additional server-side keys in a local .env file if needed.
# .env* is excluded from version control by the project .gitignore.
